---
apiVersion: automationportal.aap.redhat.com/v1alpha1
kind: AutomationPortal
metadata:
  name: portal
  namespace: self-service-portal
  annotations:
    argocd.argoproj.io/sync-wave: "6"
spec:
  aap:
    checkSSL: false
    credentials:
      secretRef: secrets-rhaap-portal
  # Omit scm.credentials when GitHub/GitLab integration is disabled — the operator
  # validates secrets-scm for GitHub App keys if secretRef is set.
  auth:
    providers:
      github:
        enabled: false
      gitlab:
        enabled: false
  plugins:
    registry: registry.redhat.io
    customAppConfigRef: portal-org-override
    catalog:
      enabled: true
      syncInterval: 60
      jobTemplates:
        enabled: true
      collections:
        enabled: false
      gitContents:
        enabled: false
  permissions:
    enabled: true
    adminUsers:
      - user:default/admin
      - group:default/aap-admins
    superUsers:
      - user:default/admin
  backstage:
    replicas: 1
    route:
      enabled: true
      host: redhat-rhaap-portal-self-service-portal.apps.ocp001.lennysh.net
  database:
    enableLocalDb: true
